/**
 * Fonte única de verdade para o tamanho das páginas das listagens da frota.
 *
 * Centraliza o valor para evitar "números mágicos" espalhados pelos widgets
 * (veículos, modelos e marcas), mantendo o comportamento consistente e fácil de
 * ajustar em um único lugar.
 */
export const FLEET_LIST_PAGE_SIZE = 3;
