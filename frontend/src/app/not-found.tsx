import { AppShell } from '@/widgets/layout/app-shell';
import { NotFoundScreen } from '@/widgets/not-found/not-found-screen';

export default function NotFound() {
  return (
    <AppShell>
      <NotFoundScreen />
    </AppShell>
  );
}
